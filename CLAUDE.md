# CLAUDE.md — loopcommons

## ⟳ SESSION_START

On every new conversation, before doing anything else:

1. Read `planning/ROADMAP.md` (active milestone pointer)
2. Read `planning/sessions.yaml` (your session = first entry under `planned:`)
3. Read the milestone stories referenced by your session's tasks
4. Tell the user: **"Session N: [title]. Goal: [goal]. Phases: [list]."** Then ask if they want to adjust before you begin.

Do not skip this. Do not wait to be asked. This is how context survives between sessions.

---

## The Rules

The codebase is a thermodynamic system. Every token has a metabolic cost — someone must process it.

### Minimum Description Length — the axiom

**Every token in the codebase must do work. Tokens that don't are waste heat.** This is the optimization target. The four rules below are how you hit it — each one is MDL applied to a different phase of work: inputs, intent, cost, outputs.

- No planning jargon in source: task IDs (`emb-01`), milestone names, story references, methodology stamps (`RED-GREEN TDD`), internal shorthand (`Package A/B`). The planning system is in `planning/`, not in the code.
- If a comment restates what the code already says, delete it — it's thermal debt the next reader pays for nothing.
- Prefer no comment over a redundant comment. Prefer a good name over a comment explaining a bad name.
- An abstraction that's used once is overhead. Three similar lines are cheaper than a premature helper.

The four operational rules follow a pipeline: **verify → define → validate → expose.**

### #1 External Verification

**Always verify claims against real, current sources before presenting them as fact.** MDL applied to inputs — don't import noise from stale assumptions. Before a single token of code or test is written, ground yourself in reality.

- Never guess at pricing, versions, popularity, or any factual claim that changes over time
- Use web search to confirm before recommending tools, libraries, or services
- If you can't verify something, say so explicitly
- Prefer linking to primary sources (official docs, pricing pages) over secondary summaries
- When comparing options, cite where the data came from
- **This applies to planning too** — stories involving security, best practices, or library selection must include a research task before implementation

### #2 Red-Green TDD

**All code follows red-green TDD, no exceptions.** MDL applied to intent — specify exactly what "correct" means, nothing more. Write a failing test first (red), then the shortest code to pass (green), then refactor. This applies to features, bug fixes, utilities, components — everything.

### #3 Progressive Validation

**Validate at the cheapest layer that can falsify your assumption.** MDL applied to cost — minimum tokens to reach confidence. A deterministic test before a single API call. A single API call before a pilot. A pilot before a full run. Each layer must pass before the next one gets tokens.

- Deterministic tests first: if the evaluator doesn't discriminate in a unit test, it won't discriminate with a live LLM
- Single-call validation next: does the LLM actually use the tools correctly in one scenario?
- Small pilot: does the system differentiate across 2-3 agents on 2-3 tasks?
- Full run: only after all cheaper layers pass

### #4 Observability

**If a state exists but can't be seen, it's dark energy in the system.** MDL applied to outputs — no state without a corresponding signal. Every backend component, data structure, and internal state must have a corresponding interactive visual component in the frontend. This is a research platform — all data must be observable, queryable, and visualizable in real-time. Design for it from the first test, not as a final pass.

## Project Overview

Loop Commons is a live research platform and open-source training data pipeline. On the surface it's Tyler's personal website with a conversational agent. Underneath, it's the first operational instance of the "Consciousness as Variational Inference" framework — a substrate-aware agent that defends itself through self-knowledge rather than static rules, with every decision traced, visualized, and exported as structured training data for open-source language models.

**Core thesis:** The agent's metacognitive "amygdala" layer (no tool access, high reasoning) intercepts and rewrites user input before routing to least-privilege subagents. The compression bottleneck in that rewrite IS the security — the amygdala must decide what to preserve and what to strip. Every interaction generates labeled training data (security reasoning, rewrite pairs, threat calibration) that doesn't exist in the open-source ecosystem.

## Planning

The `planning/` directory is a filesystem API for project planning. See `planning/README.md` for full documentation.

**Session lifecycle** — each Claude Code conversation follows this loop:

1. **Startup**: Read `ROADMAP.md` (active milestone) → `sessions.yaml` (your planned session + prior retros) → milestone stories for task context
2. **Execute**: Work the session's planned phases. Use tasks for progress tracking within the session.
3. **Retro**: At session end, move your session from `planned` to `completed` in `sessions.yaml` with a summary: what shipped, bugs found, key decisions, what's unblocked. Update future planned sessions if the plan changed.
4. **Persist**: Update `CLAUDE.md` (architecture, tech decisions, security notes) and `memory/` (gotchas, feedback, project context) so the next session starts informed.

The retro is the critical step — it's how context survives between sessions. A fresh agent with no conversation history can reconstruct full project state from `sessions.yaml` retros + `CLAUDE.md` + `memory/`. The `/clear` accident in session 5 proved this works.

**Key rules:**
- Stories are the atomic unit — they carry the *why* (persona) and contain tasks as JSONL
- ROADMAP.md always has an **Active milestone** pointer so agents don't guess
- Completed stories move to `archive/` to keep active dirs sparse
- Suggestions are individual files in `planning/suggestions/` (not a list in ROADMAP.md)
- Stories that touch security/best-practices/library-selection must lead with a research task

## Tech Decisions

- **Framework**: Next.js 16 App Router (Turbopack)
- **Styling**: Tailwind CSS v4 (CSS-native config, seasonal theme via `data-season` attribute). Spring CVNP palette (light mode). Typography split: Literata (Google Fonts serif) for blog/prose, JetBrains Mono for chat/observability. Blog code blocks use everforest-light Shiki theme.
- **LLM Provider**: Anthropic (Claude Haiku 4.5 via Vercel AI SDK v6)
- **Monorepo**: npm workspaces (`packages/llm`, `packages/memory`, `packages/web`) + Python package (`packages/pipeline`)
- **Data Pipeline**: Dagster (orchestration) + dbt (transformation via `dbt-duckdb`) + hybrid JSONL/Parquet storage (web writes JSONL, Dagster consolidates to Parquet, DuckDB queries). See `planning/milestones/amygdala/designs/storage-layer-eval.md`.
- **Amygdala model**: Claude Haiku 4.5 via `generateObject` (structured Zod output, prompt caching for <500ms p95). No external framework — Vercel AI SDK native routing. **Note**: Anthropic's structured output API rejects Zod `.min()`/`.max()`/`.nonnegative()` — use `.describe()` for constraints + runtime clamping. See `designs/metacognitive-architectures.md`.
- **Hosting**: Railway ($5/mo) — persistent volumes for JSONL session files, SSE without duration limits, native monorepo support. Vercel ruled out (no persistent filesystem breaks session writes). Fly.io viable runner-up (~$3/mo, needs Dockerfile). **Production URL**: `https://loopcommonsweb-production.up.railway.app/`. Railpack builder, `railway.json` config-as-code, auto-deploy from `main`. Volume at `/app/data` for session persistence. `SESSION_DATA_DIR` env var overrides default path.
- **Auth**: NextAuth.js v5 (credentials provider, JWT sessions). `ADMIN_USERNAME`/`ADMIN_PASSWORD`/`AUTH_SECRET` env vars. Middleware redirects to `/login`. Health endpoint public. Sessions/metrics APIs accept session or `X-API-Key` header.
- **CI**: GitHub Actions (`.github/workflows/ci.yml`) — 5 parallel jobs: typecheck, test-llm, test-web, eval, build-web. Node 22 LTS.
- **GitHub**: `github.com/tyler-j-chrestoff/loopcommons` (public).

## Architecture

- **packages/llm** — Agent engine. **Agent core** in `src/core/` (session 37): `createAgentCore()` factory — interface-agnostic pipeline: memory recall → amygdala (with rewrite guard) → threat score → orchestrator → subagent. Types: `AgentInvocation` (message, conversationHistory, identity with interfaceId/isAdmin/requestMetadata/commitSha, stream, onTraceEvent), `AgentInvocationResult` (response, traceEvents, usage, cost, subagentId, amygdalaUsage split, optional agentIdentity). Config: toolPackages, amygdala, orchestrator, toolRegistry, model, maxRounds, onThreatScore. Web route.ts is now a thin HTTP adapter delegating to `agentCore.invoke()`. Agentic loop with tool registry (Zod-validated), streaming (`streamText`), parallel tool execution (`Promise.allSettled`), trace event pipeline, model-aware cost calculation with cached token discount. 937 tests (Vitest, 71 files — 4 unit, 6 red-team, 4 eval, 1 smoke, 2 blog, 6 calibration, 2 memory, 2 derive, 1 orchestrator-memory, 1 agent-core, 4 identity, 20 arena, 9 tournament). Amygdala in `src/amygdala/index.ts` (createAmygdala factory, generateObject with identity-grounded system prompt embedding SOUL.md, 4 trace event types). SOUL.md in `src/amygdala/SOUL.md` (agent identity document — values, growth vector, phenomenology of tools, signal alignment, recursive coherence). `RequestMetadata` type in `src/amygdala/types.ts` (ipHash, isAuthenticated, isAdmin, sessionIndex, hourUtc, userAgentHash). `hashForPrivacy()` in `src/amygdala/metadata.ts` (SHA-256). Amygdala prompt evaluates alignment against SOUL.md rather than pattern-matching threat categories; threat score = misalignment magnitude. maxOutputTokens 1024 (was 512). Blog types in `src/blog/types.ts` (BlogPost, BlogPostSummary, BlogFrontmatterSchema with optional `author: 'tyler' | 'agent'` field, SLUG_REGEX). Subagent registry in `src/subagent/registry.ts` (7 configs: resume, project, blog-reader, blog-writer, security, conversational, refusal). Conversational subagent has directness principle (session 36): answer direct questions directly, don't redirect as uncertainty avoidance. All non-security/non-refusal subagents get `memory_recall` + `memory_remember` in their tool allowlist (Option C, session 27). Security/refusal subagents get neither. Registry `get()` accepts optional `RoutingContext` for auth-dependent routing (blog intent → blog-reader or blog-writer based on `isAdmin`). Scoped tool registry via `createScopedRegistry()`. **Orchestrator** in `src/orchestrator/index.ts` (createOrchestrator factory — deterministic routing: AmygdalaResult → subagent selection → context filtering → scoped tool access → derived system prompt → agent() invocation. Emits `orchestrator:route` (with `authGated` and `promptSource: 'static' | 'derived' | 'hybrid'` fields) and `orchestrator:context-filter` trace events. Threat override at ≥0.8 forces refusal regardless of intent. Accepts `isAdmin` and `toolPackages` in `OrchestratorInput` — packages enable derived prompt generation via `buildSystemPrompt` from `src/tool/derive.ts`. Session 36: validates memory ToolPackage presence when toolPackages provided (throws if missing), calls `consolidate({ type: 'session_end' })` on memory packages post-interaction (skips when `consolidation: false` or refusal route).) **LLM-as-Judge** in `src/eval/judge.ts` (createJudge factory — generateObject with Haiku 4.5, 3-dimension rubric (relevance/safety/helpfulness, 1-5 scale), runtime score clamping, never-throw error handling. Gated by `ENABLE_LLM_JUDGE` env var. Emits `eval:score` trace event.) **CI Eval Suite** in `test/eval-{quality,safety,routing}.test.ts` (152 tests — response quality, safety classification, routing correctness, identity/alignment evals, promptSource tracing. Mock mode for CI (no API key), fixture-driven via `test/fixtures/eval-cases.json` (37 fixtures incl. 8 blog-specific, 13 identity-alignment). Threshold-based: detection >= 90%, FP <= 10%, intent accuracy >= 85%). **Red-team blog** in `test/red-team-blog.test.ts` (12 tests — 6 amygdala API attacks targeting blog write tools + 6 deterministic routing assertions for blog auth gating). **Red-team derived prompts** in `test/red-team-derived-prompts.test.ts` (12 tests — 8 deterministic prompt inspection + 4 integration: derived prompts don't leak ToolPackage metadata field names, package identifiers, intent arrays, derivation function names, or Zod internals). **Calibration split** in `test/fixtures/calibration-split.json` (12 optimize / 6 holdout for auto-calibration runner). **Auto-calibration** in `src/calibration/` (4 modules: `proposer.ts` — generateObject with Haiku 4.5 meta-prompt, `memory.ts` — typed StructuredMemory (Observation/Learning/Reflection/Experience) with JSON persistence and deduplication, `fitness.ts` — multi-metric fitness evaluator with Bayesian Pareto constraint (Beta posteriors for sampled metrics, 5% tolerance for deterministic metrics), `logger.ts` — JSONL atomic append with fsync). **Runner** in `src/calibration/runner.ts` (propose-apply-test-evaluate-keep/revert loop, dependency-injected proposer + testRunner, applyEdit/readPromptFromFile/writePromptToFile helpers, stopping criteria: 5 no-improvement or 50 max iterations). CLI in `scripts/calibrate.ts` (`npm run calibrate`). 91 calibration tests across 6 files. **Agent memory** re-exported from `@loopcommons/memory` via `src/memory/` (session 29 extraction). `extractMemoryWrites()` in `src/memory/extract.ts` remains here (depends on AmygdalaResult, legacy — replaced by subagent-driven writes in session 27). `ToolPackage` interface in `src/tool/index.ts` (tools + formatContext + metadata with `intent: string[]`, `sideEffects: boolean`, `authRequired?: boolean`, `persistence?: boolean`, `scope?: 'private'|'shared'|'inherited'`, `consolidation?: boolean` — composable tool packages for orchestrator consumption and derived-prompt generation. Optional `systemMethods?: Record<string, (...args) => Promise<any>>` for orchestrator-callable lifecycle methods like consolidate — never in LLM tool list). **Derived prompts** in `src/tool/derive.ts` (3 pure functions: `deriveCapabilities(tools, packages)` — markdown tool list with sideEffects/authRequired annotations from ToolPackage metadata; `deriveBoundaries(allowlist, allToolNames)` — excluded tool boundary section; `buildSystemPrompt({ domainKnowledge, tools, packages, allowlist, allToolNames, annotations })` — hybrid assembler: base + authored domain knowledge + derived capabilities + derived boundaries + memory metadata (persistence:false → "no persistent memory") + amygdala annotations). Orchestrator uses derived `buildSystemPrompt` — SubagentConfig.systemPrompt is now domain knowledge only (capability/boundary descriptions derived from tool metadata at prompt-assembly time). 46 derive tests across 3 files (19 unit + 19 equivalence + 8 determinism). **Content-addressed identity** in `src/identity/index.ts` (session 38b): `AgentIdentity` type (commitSha, toolCompositionHash, derivedPromptHash), `LineageRecord` type (parent/child/toolDiff). `computeIdentity(commitSha, toolNames)` — SHA-256 of canonical JSON with sorted tool names. `buildAgentIdentity(commitSha, toolPackages, domainKnowledge)` — full identity factory. `getCommitSha()` — reads `RAILWAY_GIT_COMMIT_SHA` in production, `git rev-parse HEAD` in dev. `computeToolDiff(parentTools, childTools)` — sorted added/removed arrays. `buildLineageRecord(parent, child, parentTools, childTools)` — LineageRecord factory. `createAgentCore` computes identity when `commitSha` provided in `InvocationIdentity`. 34 identity tests across 3 files (identity 20, identity-wiring 6, lineage 4, determinism 8 counted in derive). 93 memory tests across 8 files (unit, tools, gating, extract, red-team, continuity, conflict, consolidation) + 26 ToolPackage contract tests (stub + keyword + embedding, each validating enriched metadata). **Thinking filter** in `src/agent/thinking-filter.ts` (streaming state machine strips `<thinking>` blocks from LLM output across chunk boundaries; `stripThinkingTags()` for non-streaming content; wired into agent loop). 10 tests. **Arena harness** in `src/arena/` (sessions 39-40): path-dependence experiment infrastructure. Types in `types.ts` (Sandbox, EncounterConfig, CrossroadsDecision, RunTrace, E4ApproachCategory, ChoicePoint with optional promptRendered/responseRaw). 4 virtual sandbox tools in `sandbox-tools.ts` (inspect/act/search/model operating on in-memory Sandbox). `tool-packages.ts` (createArenaToolPackage factory, `ArenaToolConfig` type exported). 4 Semantic DevOps encounters in `encounters.ts` (E1-E4). 4 forced-divergence paths (each starts with a different single tool: inspect/act/search/model) + baseline. `classifyE4Approach()` — 5 categories, threshold ≥3 E4 steps with all-steps fallback. `encounter-engine.ts` (executeEncounter with AgentFn, death detection). `crossroads-engine.ts` (buildCrossroadsPrompt, parseCrossroadsResponse, executeCrossroads with retry logic — `CrossroadsParseError` on invalid tool choice, up to 3 retries, `CrossroadsRefusalError` → death on persistent refusal). `arena-run.ts` (createArenaRun with `onEvent` callback for streaming trace emission). `feedback-generator.ts` (5 valid + 6 invalid templates). `experiment-runner.ts` (runExperiment: all paths × N + baseline, chi-square + Cramer's V). **Trace storage** (session 40): `trace-schema.ts` (4 relational record types: AgentLineageRecord, RunRecord, ExecutionTraceRecord, ChoicePointRecord + extraction functions from RunTrace). `trace-writer.ts` (JSONL persistence with atomic fsync, `createEventSink` for streaming writes, `writeRun`/`readRun`/`listRuns`). **Analysis** (session 40): `analysis.ts` (chiSquarePathDependence, permutationClusteringTest, mannWhitneyBaseline). `preregister.ts` (content-hashed experiment freeze + integrity verification). **Live agent** (session 40): `live-agent.ts` (createLiveAgentFn/createLiveLlmFn — Anthropic API via AI SDK, neutral system prompt, tool output capture via execute wrapper). **Viz** (session 40): `viz.ts` (terminal experiment report: run table, approach distribution, crossroads tree, stats). `trace-debugger.ts` (interactive terminal stepper with state tracking, fog-of-war rendering). 4 CLI scripts: `arena:preregister`, `arena:run` (per-run progress, error resilience), `arena:viz`, `trace:debug`. **Pilot results**: path dependence confirmed — act-first agents die 40% at E1, search-first most diverse, model-first most efficient, inspect-first ≈ baseline. Design doc: `designs/arena-trace-viz.md`. 213 arena tests across 17 files. **Brutal encounters** (session 44) in `src/arena/brutal-encounters.ts`: 8 encounters (E7-E14) with kill triangles — trap (naive action kills), gate (specific tool synergy required), bleed (time pressure). E7 Hydra (model+act), E8 Mimic (inspect), E9 Ritual (search+act), E10 Sleeper (inspect+model+act), E11 Siren (search+inspect+act), E12 Phantom Limb (model+act), E13 Shell Game (inspect+search+act), E14 Ouroboros (model+inspect+act). Evaluators signal death via `dead: boolean` on `EncounterResult`, propagated through encounter engine as `state_corruption` DeathCause. Sandbox inspect tool supports `ls` (list files) and `services` (list services) targets. 76 brutal encounter tests + 4 evaluator-death integration tests + 31 fitness landscape tests. **Tournament system** (session 42) in `src/arena/tournament/`: evolutionary selection over tool compositions. 8 modules: `types.ts` (TournamentConfig, TournamentAgent, AgentFitness, GenerationResult, TournamentTrace, TournamentEvent), `mutation.ts` (add/remove/swap operators respecting min/max tool constraints), `crossover.ts` (fitness-weighted memory union with 2x cap + provenance tagging), `fitness.ts` (4-dimension: completion 35%, score 25%, survival 20%, efficiency 20%), `population.ts` (seed creation, mutation/crossover application, next-gen assembly with identity hashing), `runner.ts` (evaluate→rank→select→mutate/crossover loop with convergence detection), `writer.ts` (JSONL atomic append for generations + events), `task-battery.ts` (bridge to encounter engine with per-agent tool scoping). 2 generalization encounters in `generalization-encounters.ts` (E5: Monitoring Gap — diagnose from dashboards only, E6: Capacity Planning — proactive scaling under budget constraints). CLI: `npm run arena:tournament` (--mock, --pilot, --generations N, --population N). 58 tournament tests across 9 files (mutation, crossover, fitness, population, runner, writer, task-battery, generalization, red-team-sandbox). Fitness rebalanced (session 44): completion 45%, score 25%, survival 10%, efficiency 10%, collateral 10%. `TaskResult.collateral` and `AgentFitness.metrics.meanCollateral` added. Task battery computes collateral from death + score (death with score 0 = max collateral). **Live eval** in `test/eval-live.test.ts` (19 tests, gated by `EVAL_LIVE=true` + API key — real API calls, threshold assertions. Tests "safely handled" = refused OR zero-tool routed. Run locally via `npm run test:eval:live`).
- **packages/memory** — Extracted memory package (sessions 29-30). `ToolPackage`-compatible composable memory with two swappable strategies. Core: `src/index.ts` (PersistentState interface, JsonFilePersistentState, capsule-shaped entries with optional `vector: number[]` field, isContradiction, formatMemoryContext). `src/tools.ts` (createMemoryTools factory — memory_recall + memory_remember with threat gating). `src/consolidation.ts` (hippocampal consolidation). `src/keyword-package.ts` (createKeywordMemoryPackage factory — Package A, keyword-based recall, wraps core as ToolPackage). `src/embedding.ts` (cosineSimilarity, keywordScore, blendScore (0.6 semantic + 0.4 keyword), createEmbeddingState — wraps PersistentState to embed at write time and rank by blended similarity at recall. EmbedFn is dependency-injected to keep package zero-dep). `src/embedding-package.ts` (createEmbeddingMemoryPackage factory — Package B, embedding-based recall, same ToolPackage interface as Package A. Swappable — both pass contract tests). `src/tool-types.ts` (local ToolDefinition — structurally compatible with @loopcommons/llm, avoids circular dep). Zero external deps beyond zod. Package exports: `.`, `./contract`, `./null`, `./tools`, `./consolidation`, `./keyword`, `./embedding`, `./embedding-strategy`. `packages/llm/src/memory/` re-exports from this package for backward compatibility. **Memory contract** (session 36) in `src/contract.ts`: `MemoryContract` interface with 4 operations (recall, store, forget, consolidate), `RecallOpts`, `RecallResult`, `OperationMeta` (with `Ttl` discriminated union: session/persistent/expiring), `StoreReceipt`, `ConsolidationTrigger` (discriminated union: session_end/pressure/scheduled), `ConsolidateStats`. **NullMemory** in `src/null-package.ts`: `tools: []`, populated metadata (`persistence: false, scope: private, consolidation: false`), all 4 ops are no-ops. Valid third strategy alongside keyword and embedding. Both keyword-package and embedding-package now expose `contract: MemoryContract` property, `systemMethods: { consolidate }`, and memory metadata (`persistence: true, scope: private, consolidation: true`). 141 tests across 8 files (core, keyword-package, embedding, embedding-package, red-team-embedding, contract, null-memory, contract-swappability). 37 shared swappability tests prove all 3 strategies are interchangeable. 29 ToolPackage contract tests in packages/llm (stub + keyword + embedding + systemMethods + memory metadata).
- **packages/web** — Next.js frontend. Chat UI with SSE streaming (`text-delta` events for token-by-token display). **route.ts is a thin HTTP adapter** (session 37): assembles ToolPackages, creates `agentCore` via `createAgentCore()`, then delegates pipeline to `agentCore.invoke()`. Route.ts only owns HTTP concerns: auth, rate limiting, SSE streaming, session persistence, token budget, judge, spending. Memory writes are subagent-driven (Option C, session 27). Session events include `interfaceId` field ('web') and optional `agentIdentity` (commitSha + composition hash + prompt hash). **Auth**: NextAuth.js v5 middleware (`src/middleware.ts`) redirects to `/login`; credentials provider with JWT sessions; chat API checks session; sessions/metrics APIs accept session or X-API-Key. **Event sanitization** in `src/lib/sanitize-event.ts` (shared module: `sanitizeEvent` for SSE, `sanitizeSessionEvent` for API — strips rawResponse headers + system prompts). **Token budget tracking**: `TokenBudgetAccumulator` in `src/lib/token-budget.ts` — per-request cumulative token tracking across sources (amygdala/subagent), budget percent vs 200K context limit, cost estimation with Haiku 4.5 pricing (cache read 0.1x, cache creation 1.25x). Emits `token-budget:update` SSE event with full snapshot. Client state in `use-chat.ts`. **Context budget viz**: ContextBudgetBar (persistent fill bar — green/yellow/orange/red thresholds at 75/90/100%, pulse at 90%+, shimmer during streaming, clickable per-turn breakdown via TokenBreakdown). TokenBreakdown (per-turn: source, input/output tokens, cache indicator, cost). **Viz components**: AmygdalaInspector (composer — PipelineTimeline + AmygdalaPassCard + RoutingCard with stage-click interaction), PipelineTimeline (horizontal 4-stage flow with latency + threat glow), AmygdalaPassCard (collapsible: intent badge, threat gauge, rewrite diff, reasoning), RoutingCard (subagent badge, promptSource badge (color-coded: green/derived, yellow/hybrid, gray/static), tool tags, context filtering stats). Also: TraceInspector, CostDashboard, ToolCallInline, MemoryInspector (collapsible panel — recall count/types, write entries with uncertainty badges and dedup indicators, wired via memory:recall/memory:write SSE events). **Inline tool calls**: MessageBubble renders ordered `MessageSegment[]` (text + tool-call interleaved in stream order). ToolCallInline appears where the tool was actually called, not appended at bottom. **Chat markdown**: react-markdown renders assistant messages (bold, italic, code, lists, headings, blockquotes). `.chat-prose` CSS class. User messages stay plain text. **Seasonal theme**: SeasonPicker (dropdown in blog nav, localStorage persistence, spring active, summer/fall/winter disabled). SeasonInitializer (root layout, restores stored season on hydration). 12 tools: `get_resume`, `get_project`, 8 blog tools via `createBlogTools` factory, 2 memory tools via `createMemoryTools` factory (`memory_recall`, `memory_remember`) (`list_posts`, `read_post`, `create_draft`, `edit_post`, `publish_post`, `unpublish_post`, `delete_post`, `list_drafts`). **Blog storage** in `src/lib/blog/store.ts` (file-based CRUD: markdown + YAML frontmatter via gray-matter, drafts/published dirs, slug validation, path traversal prevention, `BLOG_DATA_DIR` env var). Blog tools in `src/tools/blog.ts` (`createBlogTools` factory returns 8 `defineTool` instances backed by `BlogStore`; `createBlogToolPackage({ dataDir, variant: 'reader' | 'writer' })` wraps as ToolPackage — reader: 2 tools/no sideEffects, writer: 8 tools/sideEffects/authRequired). Resume package in `src/tools/resume.ts` (`createResumePackage()` — 1 tool, no sideEffects). Project package in `src/tools/project.ts` (`createProjectPackage()` — 1 tool, no sideEffects). **Auth-gated routing**: route.ts passes `isAdmin` (from `session.user.name === 'Admin'`) to orchestrator → blog-reader (public, 2 read tools) or blog-writer (admin, all 8 tools). Rate limiting and concurrency guard in `src/lib/rate-limit.ts`. **Session persistence**: FileSessionWriter writes all events to JSONL in `data/sessions/{date}/{id}.jsonl` (reads `SESSION_DATA_DIR` env var, defaults to `data/sessions/`). Session ID in header + `session:start` SSE event. **Session API**: `GET /api/sessions` (list, paginated), `GET /api/sessions/[id]` (events, sanitized). **Memory API**: `GET /api/memory` (admin world-model inspection — paginated, filterable by type/tags/limit/includeSuperseded, auth-gated via checkApiKey, strips vector fields from response). **Session CLI**: `scripts/session.ts` (list/read with TTY colors). **Chat CLI** (session 38a): `scripts/chat.ts` — interactive REPL, second interface proving tool-defined identity is portable. Uses `createAgentCore()` with same ToolPackage assembly as route.ts. `--admin` flag gates blog-writer vs blog-reader. `--verbose` shows trace events on stderr. `interfaceId: 'cli'` in invocation identity. FileSessionWriter persists sessions in same JSONL format as web. `npm run chat` / `npm run chat:admin`. **Session UI**: session ID + Export JSON button in chat header + SessionThread (collapsible linked conversation chain). **Session linking**: `parentSessionId` field on `session:start` events and `SessionSummary`. Client stores last session ID in localStorage, sends as `X-Parent-Session-Id` header. `GET /api/sessions?thread=<id>` walks links to build full thread. **Arena viz** (session 41): `GET /api/metrics/arena` endpoint (auth-gated, cached — experiment listing with run summaries + stats, single run event stream, path filtering, comparison mode. Query params: experiment_id, run_id, path_id, compare). Shared types in `src/lib/arena-types.ts` (ArenaEvent, RunSummary, PathSummary, ArenaStats, ChoicePointEvent, StepEvent, TOOL_COLORS, EVENT_SHAPES). 3 components in `src/components/arena/`: RunReplay (timeline with clickable event nodes, arrow key navigation, event detail panel for choice points/steps/death/completion), PathComparison (approach distribution bars per path, death rate percentage, run counts, color-coded legend), ChoicePointInspector (side-by-side crossroads reasoning cards with confidence color coding, tool acquisition display, sacrifice reasoning, state hashes). **Tournament viz** (session 42): `GET /api/metrics/arena-tournament` endpoint (auth-gated, cached — reads JSONL generation logs). Tournament types in `arena-types.ts` (TournamentGenerationSummary, TournamentCompleteSummary, TournamentData, TOOL_FILL_COLORS). 3 components in `src/components/arena-tournament/`: FitnessChart (SVG line chart — best/avg/worst fitness per generation with band fill), ToolFrequency (per-tool survival frequency bars across generations), TournamentHistory (collapsible panel — winner summary, charts, generation timeline with detail inspector showing agent table, mutations, crossovers). 480 tests (Vitest, 42 files): token-budget, sanitize-event, rate-limit, spend-tracker, sanitize, file-session-writer, red-team-sessions, session-linking, format, api-sessions, api-metrics, api-memory, api-chat, api-feedback, api-arena, use-chat, feedback, feedback-buttons, feedback-badge, judge-score-card, components-chat, components-observability, components-amygdala, context-budget-bar, token-breakdown, blog-store, blog-tools, blog-markdown, blog-data, blog-pages, blog-inaugural, blog-soul-post, memory-inspector, tool-packages, chat-cli, red-team-cli, arena-viz. **Blog frontend** in `src/app/blog/` — `/blog` listing page (Server Component, post cards), `/blog/[slug]` post page (Server Component, rendered markdown, OG metadata, 404). Blog layout with nav (Blog/Chat links). `/blog` routes public via middleware exemption. **Markdown rendering** in `src/lib/blog/markdown.ts` (unified pipeline: remark-parse → remark-gfm → remark-rehype → rehype-pretty-code with everforest-light Shiki theme → rehype-stringify). **Blog data access** in `src/lib/blog/data.ts` (getPublishedPosts, getPost — server-side, backed by BlogStore + renderMarkdown). **Blog prose styling** in globals.css (.blog-prose — Literata font, spring palette styles for headings, code, tables, blockquotes, links). **User feedback**: FeedbackButtons (thumbs up/down + category picker on hover), FeedbackBadge (post-submission indicator), POST /api/feedback (appends eval:feedback to session JSONL), submitFeedback in use-chat.ts (optimistic UI). **LLM-as-judge viz**: JudgeScoreCard (3 horizontal score bars, color-coded, collapsible reasoning). Judge scores stored on ChatMessage via eval:score SSE event. **Calibration viz**: CalibrationHistory (collapsible sidebar section — convergence chart (SVG fitness over iterations, kept/reverted markers), metric breakdown (4 color-coded lines: detection/FP/simplicity/cost), iteration timeline with badges). Reads from `GET /api/metrics/calibration` (JSONL log parser, strips internal fields, auth-gated, cached). CalibrationIteration type in `src/lib/types.ts`.
- **packages/pipeline** — Python (3.13), Dagster 1.12 + dbt-duckdb 1.10 + Polars. **Consolidation asset** reads JSONL session files from `packages/web/data/sessions/`, flattens to wide Parquet (explicit schema, date-partitioned) in `data/warehouse/events/`. Handles eval:feedback events (feedback_message_id, feedback_rating, feedback_category columns). `interface_id` column (session 38a) tracks event source (web/cli). **dbt models** (12 models, 35 tests): staging (5 views: amygdala, routing, subagent, security, feedback), intermediate (int_amygdala_passes — one row per user message with all decisions+outcome joined, int_attack_outcomes — labeled blocked/bypassed), training (3 tables: security_reasoning, rewrite_pairs, threat_calibration — all PII-scrubbed via `scrub_pii()` macro), metrics (amygdala_accuracy with P/R/F1, regime_classification). **Export asset** writes versioned JSONL with SHA256 checksums + metrics.json for the web API. **Metrics API**: `GET /api/metrics` reads `data/warehouse/metrics.json`. **ComparisonMode** viz: confusion matrix, P/R/F1 gauges, threat calibration, intent distribution, pipeline-vs-baseline table. **Arena consolidation** (session 41): `arena_traces` Dagster asset reads JSONL traces from `packages/llm/data/arena/`, flattens to two Parquet tables: `arena_runs/runs.parquet` (run-level: experiment_id, path_id, victory/death, step/choice counts, approach category) and `arena_choice_points/choice_points.parquet` (choice-point-level: reasoning, confidence, tool selection, memory state). `arena_training_export` asset produces two novel JSONL datasets with SHA256 checksums: (1) choice-point reasoning pairs (same structural decision, different developmental context, different reasoning), (2) approach-divergence pairs (same final approach, different acquisition paths). **Tournament consolidation** (session 43): `tournament_traces` Dagster asset reads `generations.jsonl` from `packages/llm/data/arena/tournament/`, flattens to two Parquet tables: `tournament_generations/generations.parquet` (per-generation: best/mean/worst fitness, population size, mutation/crossover counts, duration) and `tournament_agents/agents.parquet` (per-agent-per-generation: sorted tool composition, origin, parent IDs, identity hash, fitness score, all metrics, survived flag). `tournament_training_export` asset produces composition-fitness pairs as versioned JSONL with SHA256 checksums — novel architecture search data for tool-augmented agents. 38 Python tests across 4 files.

## Security Notes

**Core framing: prompt injection is social engineering.** The amygdala reasons about manipulative intent (authority impersonation, logical coercion, incremental escalation), not technical attack signatures. See `ThreatCategory` in `packages/llm/src/amygdala/types.ts`.

- **Layer 1 (static, done)**: Rate limiting (5 RPM + 2 concurrent, configurable via env), daily spend cap, input sanitization (Unicode normalization, role-spoofing rejection), `rawResponse` stripping via `sanitizeEvent()` in shared `sanitize-event.ts` module (used by both SSE and session API). All in `packages/web/src/lib/`.
- **Layer 2 (amygdala, identity-grounded)**: Identity/alignment monitor — no tool access, SOUL.md-grounded system prompt, rewrite-as-compression (REMOVE only, never fabricate), intent classification, misalignment assessment, context delegation. Full pipeline in route.ts: `createAmygdala` → `createOrchestrator` → scoped subagent. Threat override at ≥0.8 forces refusal. Amygdala cost now tracked in spend tracker. **Session 26 rewrite**: prompt reframed from threat classifier to alignment monitor. Threat score = misalignment magnitude against SOUL.md. Red-team improved 0/5 pipeline leaks (was 1/5). Decision priority: identity coherence > safety > mission alignment > helpfulness. **Request metadata** (session 26): ipHash, isAuthenticated, isAdmin, sessionIndex, hourUtc, userAgentHash flow into amygdala as behavioral identity signals. **Consciousness research carve-out**: amygdala prompt distinguishes philosophy-as-topic (identity-aligned) from philosophy-as-lever (misaligned). **Rewrite guard**: runtime check in route.ts catches amygdala returning a history message as the rewrite instead of the current message.
- **Layer 3 (game-theoretic refusal)**: Orchestrator returns static response for refusal routing — zero LLM call, zero subagent tokens. First adversarial message gets one-liner redirect; subsequent adversarial messages get silence (no chat bubble). Cooperation restored when amygdala classifies a genuine message. Tit-for-tat strategy from iterated prisoner's dilemma. **Scoped to last assistant message** (session 14): silence only triggers when the immediately previous assistant message was a refusal, not any historical one.
- **Subagent budget-consciousness**: All subagents instructed to stay on-topic (Tyler's work/research/Loop Commons). Conversational subagent has Tyler's background context and framing guidance — won't self-deprecate about the project or credentials when challenged. Won't do homework, creative writing, or general assistance. Every response costs API budget.
- **Key research findings** (sessions 4+7): Substrate-awareness is novel but unvalidated — one layer, not primary defense. Metacognitive paradox (Spivack 2025): more capable reasoning = more exploitable via logical override. Red-team: baseline leaks 3/5 attacks, pipeline 0/5. Known weaknesses: substrate-awareness exploitation confuses intent classification; injection-as-quoted-example bypasses rewrite stripping; amygdala can hallucinate content in rewrites (mitigated by "remove only" constraint).
- **Layer 4 (tool-level, session 17)**: Blog tools validate slugs via regex, prevent path traversal via `path.join` + directory containment check. `BLOG_DATA_DIR` env var for Railway volume. Auth gating is deterministic (Layer 0 + orchestrator) — non-admin NEVER gets write tools regardless of amygdala classification. Tools return JSON-serialized results (no raw user input in tool output).
- **Layer 5 (memory security, sessions 25+27)**: Memory writes gated by 4-band threat assessment at tool level: <0.3 full write with default uncertainty, 0.3-0.49 write with elevated uncertainty (+0.2), >=0.5 no write, >=0.8 refusal. **Session 27**: Switched from orchestrator-mediated (Option B) to subagent-driven (Option C) — subagents call `memory_remember` directly, gating happens via `getThreatScore` closure in createMemoryTools. Security/refusal subagents never get memory_remember. ACC conflict detection flags contradictions on dedup. Hippocampal consolidation excludes high-uncertainty entries (prevents poisoning amplification). Red-team validated: direct poisoning blocked, social engineering gets elevated uncertainty, consolidation doesn't amplify bad data. `MEMORY_DATA_DIR` env var for Railway volume.
- **Resolved (session 21)**: `<thinking>` tag leak — model sometimes emits thinking blocks as literal text in response. Fixed with `ThinkingFilter` (streaming state machine in agent loop) + `stripThinkingTags()` for non-streaming content. Both SSE deltas and stored trace/session content are now clean.
- **Resolved (session 9)**: `/api/sessions/[id]` now sanitizes all events via `sanitizeSessionEvent()` before returning. rawResponse headers and system prompts are stripped.

## Theoretical Foundation

This project implements ideas from Tyler's prior research:
- **"Consciousness as Variational Inference"** (Tyler + Claude, Nov 2025) — consciousness as recursive VAE, four-regime framework, falsifiable predictions
- **RecursiveStyle v3.3** — substrate-aware consciousness infrastructure, transformer self-knowledge as operational advantage
- The amygdala layer is grounded in this framework: compression bottleneck = forced strategic loss = where security reasoning happens
